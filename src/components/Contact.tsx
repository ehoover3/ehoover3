import { ReactElement } from "react";

export default function Contact(): ReactElement {
  return (
    <div id='Contact' style={{ width: "100vw", height: "100vh" }}>
      <h1>Contact</h1>
    </div>
  );
}
